package com.machrist.mpmonitoring.metric.mp

import org.apache.coyote.http11.Constants.a

class MatrixProfileService {
    fun stomp() {
        var a : ArrayList<Int> = arrayListOf(1, 2)
        a.size
    }
}
